version: '3.8'

services:
  postgis:
    image: postgis/postgis:16-3.4
    container_name: sih26_postgis
    environment:
      POSTGRES_USER: sih_user
      POSTGRES_PASSWORD: sih_password
      POSTGRES_DB: sih_db
    ports:
      - "5432:5432"
    volumes:
      - postgis_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: sih26_redis
    ports:
      - "6379:6379"

  kafka:
    image: bitnami/kafka:3.6
    container_name: sih26_kafka
    ports:
      - "9092:9092"
    environment:
      - KAFKA_CFG_NODE_ID=0
      - KAFKA_CFG_PROCESS_ROLES=controller,broker
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=0@kafka:9093
      - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER

  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: sih26_fastapi
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://sih_user:sih_password@postgis:5432/sih_db
      - REDIS_URL=redis://redis:6379/0
      - KAFKA_BOOTSTRAP_SERVERS=kafka:9092
    depends_on:
      - postgis
      - kafka
      - redis

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    container_name: sih26_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  postgis_data:
